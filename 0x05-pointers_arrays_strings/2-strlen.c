#include "main.h"
#include<string.h>

/**
 * _strlen - return length of string
 * *@s: pointer to char
 *
 * Return: return an integer
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
